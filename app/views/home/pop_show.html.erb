<div class="entry o_show">
	<%=image_tag(@p.image.url, :size => '200x150', :algin => 'left', :border => 0, :style => "margin-right:15px ")%>
	<h3><%=@p.name%></h3>
	<p><%=@p.description%></p>
	<div class="clear"></div>
	<h4>习性特征</h4>
	<div>
		<%=@p.behaviour%>
	</div>
	<h4>象征意义</h4>
	<div>
		<%=@p.meaning%>
	</div>
		<%=link_to "返回", root_path, :style => "position: relative; left: 900px;"%>
</div>

