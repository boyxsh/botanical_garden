<%@products.each do |pop|%>
<div class="entry">
	<%=image_tag(pop.image.url, :size => '200x150', :algin => 'left', :border => 0, :style => "margin-right:15px ")%>
	<h3><%=pop.name%></h3>
	<p><%=pop.description%></p>
	<%=link_to 'Add to Cart', '/add_item/'+pop.id.to_s%>
</div>
<%end%>