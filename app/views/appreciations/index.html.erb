
<h1>植物欣赏 <div class="serach">searchs</div> </h1>
<div class="container_16 xsh">
<% @appreciations.each do |appreciation| %>
  <div class="grid_3 appreciation_index ">
    <td><%= link_to image_tag(appreciation.avatar.url(:thumb)), :action => 'show', :id => appreciation%>
      <p><%= appreciation.name %> <em><%= appreciation.category.name %></em></p></td>
  </div>

<% end %>

</div>